<p id="notice"> <%= notice %> </p>

<div class="container">
  <div class="row">
    <div class="col-md-4 col-md-offset-4">
      <div class="">
        <%= image_tag @post.image, class: "thumbnail" %>
      </div>
    </div>
  </div>
</div>

    <div>
      <p>
        <strong>Title:</stong>
          <%= @post.title %>
      </p>

      <p>
        <strong>Content:</stong>
          <%= @post.content %>
      </p>

      <%= form_for @comment, url: post_path(@post) do |f| %>
        <%= f.text_area :body %>
        <%= f.submit 'Add Comment' %>
      <% end %>

      <ul>
        <% @post.comments.each do |c| %>
          <li><%= c.body %> --- Posted by: <%= c.user.name if c.user %> </li>
        <% end %>
      </ul>

      <%= link_to 'Edit', edit_post_path(@post) %> |
      <%= link_to 'Back', posts_path %> |
      <%= link_to 'Destroy', @post, method: :delete, data: { confirm: 'Are you sure?' } %></li>
  </div>
